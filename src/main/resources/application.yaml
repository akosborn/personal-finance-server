spring:
  application:
    name: perfi
  datasource:
    url: jdbc:mysql://localhost/perfi_two?autoReconnect=true&useSSL=false
    username: root
    password: pw
  jpa:
    hibernate:
      ddl-auto: update
    Show-sql: false
